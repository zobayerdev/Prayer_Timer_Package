name: prayer_timer_example
description: An example app for the prayer_timer package.
publish_to: 'none'

environment:
  sdk: '^3.7.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  prayer_timer:
    path: ../
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true